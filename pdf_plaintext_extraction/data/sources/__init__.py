"""Source texts for the synthetic-gold benchmark.

These plaintext files ship inside the wheel and are the canonical
inputs to ``synthesize.clean_pdf`` and ``synthesize.poison``. Access
them via ``importlib.resources.files(__name__)``."""
